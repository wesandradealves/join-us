import React, { useCallback, useContext } from 'react';

import {  } from './styles';

import {
  Wrapper
} from '../../layouts/styles';

const LoginPage: React.FC = () => {
	return (
		<Wrapper> 
			<>Login</>
		</Wrapper>
	);
};

export default LoginPage;
