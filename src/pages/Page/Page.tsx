import React, { useCallback, useContext } from 'react';

import {  } from './styles';

import {
  Wrapper
} from '../../layouts/styles';

const DefaultPage: React.FC = () => {
	return (
		<Wrapper> 
			<>DefaultPage</>
		</Wrapper>
	);
};

export default DefaultPage;
